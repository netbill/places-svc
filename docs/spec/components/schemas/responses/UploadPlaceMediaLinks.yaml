type: object
required:
  - data
  - included
properties:
  data:
    type: object
    required:
      - id
      - type
      - attributes
      - relationships
    properties:
      id:
        type: string
        format: uuid
        description: "place id"
      type:
        type: string
        enum: [ place_upload_links ]
      attributes:
        type: object
        required:
          - icon
          - banner
        properties:
          icon:
            $ref: './UploadResourcesLink.yaml'
          banner:
            $ref: './UploadResourcesLink.yaml'
      relationships:
        type: object
        properties:
          place:
            type: object
            required:
              - data
            properties:
              data:
                type: object
                required:
                  - id
                  - type
                properties:
                  id:
                    type: string
                    format: uuid
                    description: "place id"
                  type:
                    type: string
                    enum: [ place ]

  included:
    type: array
    items:
      $ref: './PlaceData.yaml'
