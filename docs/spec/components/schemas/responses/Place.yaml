type: object
required:
  - data
properties:
  data:
    $ref: './PlaceData.yaml'

  included:
    type: array
    description: "Included related resources (e.g., organization)"
    items:
      oneOf:
        - $ref: './OrganizationData.yaml'
        - $ref: './PlaceClassData.yaml'