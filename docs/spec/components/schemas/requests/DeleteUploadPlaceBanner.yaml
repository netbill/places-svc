type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - id
      - type
      - attributes
    properties:
      id:
        type: string
        format: uuid
        description: "place id"
      type:
        type: string
        enum: [ place ]
      attributes:
        type: object
        required:
          - banner_key
        properties:
          banner_key:
            type: string
            description: "icon media key"
