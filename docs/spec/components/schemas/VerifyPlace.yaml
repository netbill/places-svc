type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ update_place_verify ]
      attributes:
        type: object
        required:
          - verify
        properties:
          verify:
            type: boolean
            description: "Indicates whether the place has been verified"
